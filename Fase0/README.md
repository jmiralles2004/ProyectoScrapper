# ⚠️ FASE 0 - BACKUP INMUTABLE

Esta carpeta contiene:
- `_backup_phase0_base/` - Respaldo de la infraestructura inicial (NUNCA MODIFICAR)

Los archivos de trabajo se encuentran en la **raíz del proyecto**:
- `/docker-compose.yml`
- `/init-db.sql`
- `/nginx-proxy/`
- `/.env` y `/.env.docker`

Ver `../_backup_phase0_base/README.md` para más detalles.
